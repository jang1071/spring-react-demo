spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devlog_user?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1234  # ← docker에서 설정한 비밀번호
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081
